package com.meno.app

import android.app.Application

class MenoApplication : Application() {
    override fun onCreate() {
        super.onCreate()
        // Future application-level initialization (Room, DataStore, WorkManager, etc.)
    }
}
